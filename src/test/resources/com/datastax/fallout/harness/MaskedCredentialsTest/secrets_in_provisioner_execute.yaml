ensemble:
  server:
    node.count: 1
    provisioner:
      name: local
    configuration_manager:
      - name: secret_using_cm

  client: server

workload:
  phases:
    - noop:
        module: noop

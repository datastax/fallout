ensemble:
    server:
        node.count: 1
        provisioner:
            name: local
            my_invalid_key: false
        configuration_manager:
            name: noop
    client: server
workload:
    phases:
        - my_module:
              module: noop

apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{name}}
spec:
  storageClassName: fallout-storage
  capacity:
    storage: {{capacity}}
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Recycle
  hostPath:
    path: {{path}}